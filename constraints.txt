# Pinned resolution of the ops-engine build set, generated with:
#   uv pip compile pyproject.toml --no-annotate --no-header
# Two builds from the same tag install exactly these versions; without these
# exact pins, the `>=` floors in pyproject.toml resolve differently over time
# and produce two different wheels. This file is committed and is the single
# input to both the isolated build and the SBOM.
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
pydantic==2.13.5
pydantic-core==2.46.5
pyyaml==6.0.3
typing-extensions==4.16.0
typing-inspection==0.4.4
